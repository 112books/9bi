# Mòdul taro — bundle local (PAS 1) — PROVA FUNCIONAL PASSADA

### Test WSGI local (2026-09-20) — router `modules/taro/`
- `/health` → 200 ok (router pur)
- `/autopublica/health` → 200 ok (dispatch)
- `/votacio/health` → 200 ok (dispatch)
- `/formularis/health` → 200 ok (dispatch) — formularis sense config.ini reals
- `/` → 404 (no deixa caure res)
- `/no-existeix` → 404

### Config locals (gitignored, secrets FORA de git)
- config.ini de cada submòdul al bundle: còpies del real (autopublica, votacio) i DEVs (formularis). Secrets mai a git.
