# autopublica — config d'exemple. Copia a config.ini i omple els secrets.
# Secrets NO commits: config.ini està al .gitignore.

[general]
# Secret compartit amb el webhook de Codeberg (Settings → Webhooks).
# Genera'n un amb: python3 -c "import secrets;print(secrets.token_urlsafe(24))"
secret = CANVIA-ME

[repo]
# Directori de treball on hi ha el clon del repo (branca main).
workdir = /ruta/al/repo
